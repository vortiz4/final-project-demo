import React from "react";

/**
 * This function lets the user to write an entry form or their first journal. 
 */
const NotesContext = React.createContext ({
    // currentNote: null,
    // notes: []
})

export default NotesContext;