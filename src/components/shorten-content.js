import React from "react";

/**
 * This automatically folds up or shortens the lengthy content, depending how long it is, to its minimum after the note is added.
 */
function PreExpand() {
	return;
}

export default PreExpand;
